<% @posts.each do |post|%>
<div class="post-container">
    <h2 class="title"><%= post.title %> </h2>
    <p class="body"> <%= post.body %> </p>
    <% if user_signed_in? %>
    <h4 class="author"><%=User.find(post.user_id).username%></h4>
    <% end %>
</div>
<% end %>